# ExpressBuddy Environment Variables
# Copy this file to .env and fill in your actual values. Do NOT commit .env with real secrets.

# Google Gemini AI API Key (server-only)
# DO NOT prefix with REACT_APP_. Use a server-side variable and Netlify Functions.
# Netlify variable name: GEMINI_API_KEY
GEMINI_API_KEY=your_gemini_api_key_here

# Supabase Configuration (safe for client-side)
# These are used by the frontend; REACT_APP prefix means they will be bundled into the client.
# Get these from your Supabase project settings -> API
REACT_APP_SUPABASE_URL=your_supabase_url_here
REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Notes on Migration from Kinde to Supabase:
# 1. Supabase handles authentication directly - no need for separate auth provider
# 2. Make sure to update your database schema:
#    - Rename 'kinde_user_id' column to 'user_id' in the 'children' table
#    - Update any Row Level Security (RLS) policies to use auth.uid()
# 3. Configure your Supabase Auth settings:
#    - Enable Email/Password provider in Authentication -> Providers
#    - Set up email templates in Authentication -> Email Templates
#    - Configure redirect URLs in Authentication -> URL Configuration

@jsr:registry=https://npm.jsr.io

# Suppress npm deprecation warnings during build
fund=false
audit=false
# Silent install to reduce log noise

# Use legacy peer deps to allow Vite 7 with older @types/node
legacy-peer-deps=true
